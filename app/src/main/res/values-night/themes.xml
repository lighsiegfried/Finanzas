<?xml version="1.0" encoding="utf-8"?>
<!-- Window/launch theme (dark). -->
<resources>
    <style name="Theme.MisFinanzas" parent="@android:style/Theme.Material.NoActionBar" />

    <!-- tema de arranque para modo oscuro, mismo verde de la marca -->
    <style name="Theme.MisFinanzas.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.MisFinanzas</item>
    </style>
</resources>
